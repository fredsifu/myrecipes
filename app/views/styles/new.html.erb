<%= render 'shared/page_title', title: 'New Style' %>

<%= render "shared/errors", obj: @style %>

<div class="row">
  <div class="well col-sm-8 col-sm-offset-2">
    <%= form_for @style do |f| %>
      <p>
        <%= f.label :name %><br>
        <%= f.text_field :name %>
      </p>
      <p>
        <%= f.submit "Create Style", class: "btn btn-success" %>
      </p>
    <% end %>
  </div>
</div>